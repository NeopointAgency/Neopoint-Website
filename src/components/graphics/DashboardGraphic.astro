---
/** Panel de monitoreo (versión clara): visitas, leads y citas por mes, y de dónde llega el tráfico. */
const kpis = [
  { label: 'Visitas', value: '4,820', delta: '+18%' },
  { label: 'Leads', value: '132', delta: '+24%' },
  { label: 'Citas', value: '41', delta: '+31%' },
];
const bars = [38, 46, 42, 58, 64, 60, 72, 78, 74, 88, 92, 100];
const sources = [
  { name: 'Google', pct: 52 },
  { name: 'Meta', pct: 31 },
  { name: 'Directo / recomendados', pct: 17 },
];
---
<div class="absolute inset-0 p-[5%]">
  <div class="flex h-full flex-col rounded-[3cqw] bg-paper p-[3.4cqw] shadow-[0_0_0_1px_var(--color-line),0_30px_50px_-30px_rgb(0_0_0/0.25)]">
    <div class="flex items-center justify-between">
      <span class="text-[max(0.6rem,2.8cqw)] font-semibold text-text">Tu panel</span>
      <span class="rounded-full bg-surface px-[2cqw] py-[0.6cqw] text-[max(0.5rem,2.2cqw)] text-muted">Últimos 12 meses</span>
    </div>
    <div class="mt-[2.6cqw] grid grid-cols-3 gap-[2cqw]">
      {kpis.map(({ label, value, delta }) => (
        <div class="rounded-[2cqw] bg-surface p-[2.4cqw]">
          <p class="text-[max(0.5rem,2.2cqw)] text-muted">{label}</p>
          <p class="mt-[0.6cqw] text-[max(0.8rem,4cqw)] font-semibold leading-none text-text">{value}</p>
          <p class="mt-[1cqw] text-[max(0.48rem,2cqw)] font-medium text-lime-deep">{delta}</p>
        </div>
      ))}
    </div>
    <div class="mt-[2.6cqw] grid flex-1 grid-cols-[1.5fr_1fr] gap-[2.6cqw]">
      <div class="flex items-end gap-[1cqw] rounded-[2cqw] bg-surface p-[2.4cqw]">
        {bars.map((h, i) => (
          <span class:list={['flex-1 rounded-[0.6cqw]', i === bars.length - 1 ? 'bg-lime' : 'bg-ink/20']} style={`height:${h}%`}></span>
        ))}
      </div>
      <ul class="flex flex-col justify-center gap-[1.8cqw]" role="list">
        {sources.map(({ name, pct }) => (
          <li>
            <div class="flex justify-between text-[max(0.48rem,2.1cqw)] text-muted"><span>{name}</span><span>{pct}%</span></div>
            <span class="mt-[0.8cqw] block h-[1.2cqw] rounded-full bg-ink/10"><i class="block h-full rounded-full bg-ink/70" style={`width:${pct}%`}></i></span>
          </li>
        ))}
      </ul>
    </div>
  </div>
  <span class="g-tile absolute left-[2%] bottom-[5%] -rotate-[8deg] [--tilt:-8deg]" aria-hidden="true">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M4 20V4M4 20h16" /><path d="m7 15 4-5 3 3 5-7" /></svg>
  </span>
</div>
